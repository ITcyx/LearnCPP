#include <iostream>

int main()
{
	int* aip, ai, &ar = ai;  // aip is a int pointer, ai is a int object, ar is a int reference whitch is refering to ai
	int bi, *bip = 0;  // bi is a int object, bip is a int pointer
	int* cip, cip2;  // cip is a int pointer cip2 is a int object
	return 0;
}